import React from 'react';
/**
 * This is just a test functional component
 * it does quite nothing :D
 */

function Timer(props) {
    return (
        <div>{props.time}</div>
    )
}

export default Timer;